{
  "name": "@brutal/render",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": { "types": "./dist/index.d.ts", "default": "./dist/index.js" } },
  "files": ["dist"],
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json"
  },
  "dependencies": {
    "@brutal/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "comment": "Runtime deps (polotno-node, sharp, pdf-lib, @remotion/renderer) are added by the factory per docs/06."
}
